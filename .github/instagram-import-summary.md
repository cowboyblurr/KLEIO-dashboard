# KLEIO Instagram Import

Implementation branch for the read-only Instagram professional-account import flow. The feature uses Instagram Login, requests only `instagram_business_basic`, keeps access tokens encrypted and server-only, copies only artist-selected still images into private KLEIO storage, autosaves editable review records, and requires explicit artist approval before portfolio creation.
